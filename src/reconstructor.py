from typing import Optional

def recreate_with_rectangles(input_path: str, output_path: str, n_rects: int = 25) -> Optional[str]:
    """
    Toma una imagen y la recrea artísticamente usando n_rects rectángulos principales.
    Guarda el resultado en output_path.
    """
    # Aquí irá la lógica real
    print(f"Procesando {input_path} -> {output_path} con {n_rects} rectángulos.")
    # Por ahora, solo imprime (pronto implementamos el algoritmo)

    # Retorna el path de salida (o None si hay fallo)
    return output_path